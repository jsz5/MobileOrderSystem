<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobileordersystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".customer.ShowCustomerActivity"></activity>
        <activity android:name=".equipment.ShowEquipmentActivity" />
        <activity android:name=".customer.CreateCustomer" />
        <activity android:name=".order.CreateOrder" />
        <activity android:name=".equipment.CreateEquipment" />
        <activity android:name=".authorization.ChangePasswordActivity" />
        <activity android:name=".authorization.SignUpActivity" />
        <activity android:name=".authorization.ForgotPasswordActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".authorization.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!-- TODO Cloud Messaging
<service android:name=".MyFirebaseMessagingService">
    <intent-filter>
        <action android:name="com.google.firebase.MESSAGING_EVENT"/>
    </intent-filter>
</service>

<service android:name=".FirebaseIDService">
    <intent-filter>
        <action android:name="com.google.firebase.INSTANCE_ID_EVENT"/>
    </intent-filter>
</service>
-->

    </application>

</manifest>